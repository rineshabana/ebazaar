<%= link_to 'Create New Address', user_address_new_path, class: 'btn btn-primary w-100', remote: true%><br>
<br>
<div id="address-form"></div>
<div class="row" id="address-parent">
        <% @addresses.each do |address| %>
            <%= render partial:'address', locals: {addr: address}%>
        <% end %>
</div>