<%= render 'product_style'%>
<div class="bg-white">
    <div class="row">
        <%@products.each do |product|%>
            <%= render partial:'product_listing', locals: {prod: product}%>
        <%end%>
    </div>
</div>
